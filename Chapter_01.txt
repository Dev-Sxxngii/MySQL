데이터베이스는 '데이터의 집합'이라고 한다.

DBMS(Database Management System) == 소프트웨어

데이터베이스를 운용하는 소프트웨어이다.

데이터의 집합이 '데이터베이스' / 이를 관리하는 소프트웨어를 'DBMS'

DBMS 조건: 대용량 / 공유(다수의 사용자가 동시에 접근)

DBMS 발전 순서
1. 계층형 DBMS
2. 망형 DBMS
3. 관계형 DBMS(테이블로 구성[열, 행])

SQL(질의어): DBMS에서 사용하는 언어




